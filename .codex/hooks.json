{
  "description": "Prepend <repo>/bin to PATH for every Bash tool call so `bd` is the vendored build (see AGENT_INSTRUCTIONS.md). Loads only for trusted projects; Codex asks you to trust this hook once (/hooks).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "\"$(git rev-parse --show-toplevel)/scripts/agent-path-rewrite.sh\" codex",
            "timeout": 10,
            "statusMessage": "Putting ./bin first on PATH"
          }
        ]
      }
    ]
  }
}
